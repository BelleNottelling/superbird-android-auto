[Unit]
Description=OpenAuto

[Service]
ExecStart=/scripts/start_openauto.sh
# clear display when stopping, so it doesn't just freeze on the last image
ExecStopPost=/scripts/clear_display.sh
RestartSec=5

[Install]
WantedBy=multi-user.target